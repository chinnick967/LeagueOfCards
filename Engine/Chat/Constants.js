(function () {

	var PLAYER = '<span style="color:blue">{{player}}</span>';

	ChatBox.msg = {
		DESTROYED_CARD: PLAYER + ' destroyed {{card}}',
		DREW_CARD: PLAYER + 'drew a card',
		PLAYED_CARD: PLAYER + 'played {{card}}'
	};

} ());